/**
 * The {@code functions} package contains all mathematical functions used in
 * a neural network.
 * <p>
 * These can be split into {@code Activators} and {@code Losses}. The different
 * {@code Activators} all use values from {@code Neurons} to assist in
 * activating a {@code Neuron} in the next {@code Layer}. The {@code Losses}
 * are used in the final {@code Layer} to compare the predicted and actual
 * values of the output {@code Neurons}.
 * </p>
 *
 * @since 1.0.0
 * @author mghosh00
 * @version 1.0.0
 */
package neural_network.functions;